[package]
name = "qmux-proto"
version = "0.2.0"
description = "Wire types for the qmux control protocol."
license = "MIT"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
